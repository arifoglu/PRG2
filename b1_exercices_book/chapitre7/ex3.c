#include <stdio.h>

void tri(int t[],int size){

}

int main() {
    int tablo[] = {12,13,21,3,4,5,6,34,8,7};
    int size = sizeof(tablo) / sizeof(tablo[0]);
    
    tri(tablo,size);
    
    return 0;
}
